import fs from 'node:fs';
// Ten original dark ambient loops. Deterministic synthesis, no third-party recordings.
const tracks=[['case-01',36,[0,1,7],40],['case-02',38,[0,6,7],44],['case-03',33,[0,1,10],54],['case-04',37,[0,5,6],38],['case-05',31,[0,2,6],46],['case-06',35,[0,1,7],60],['case-07',34,[0,6,10],36],['case-08',39,[0,1,6],48],['case-09',32,[0,2,7],52],['case-10',36,[0,3,6],42]];
fs.mkdirSync('profiler/dist/audio',{recursive:true});
for(const[id,root,notes,bpm]of tracks){const sr=22050,duration=32*60/bpm,n=Math.round(sr*duration),pcm=Buffer.alloc(n*2),hz=m=>440*2**((m-69)/12);let seed=Number(id.slice(-2))*713,low=0;const rand=()=>{seed=(1664525*seed+1013904223)>>>0;return seed/4294967296*2-1};for(let i=0;i<n;i++){const t=i/sr,beat=t*bpm/60,fade=Math.min(1,t/2.8,(duration-t)/2.8);let v=0;low=.993*low+.007*rand();v+=low*.21;for(let j=0;j<3;j++){const f=hz(root+notes[j]);v+=.035*Math.sin(2*Math.PI*f*t+.5*Math.sin(t*.12+j))*(.65+.35*Math.sin(t*.22+j));v+=.018*Math.sin(2*Math.PI*(f+.19+j*.07)*t)}const pulse=beat%2;v+=.035*Math.sin(2*Math.PI*hz(root-12)*t)*Math.exp(-pulse*3);const bell=beat%8,k=Math.floor(beat/8);v+=.019*(Math.sin(2*Math.PI*hz(root+24+notes[k%3])*t)+.3*Math.sin(2*Math.PI*hz(root+24+notes[k%3])*2.71*t))*Math.exp(-bell*1.8)*(1-Math.exp(-bell*40));v*=fade;pcm.writeInt16LE(Math.round(Math.max(-.85,Math.min(.85,v))*32767),i*2)}const h=Buffer.alloc(44);h.write('RIFF');h.writeUInt32LE(36+pcm.length,4);h.write('WAVEfmt ',8);h.writeUInt32LE(16,16);h.writeUInt16LE(1,20);h.writeUInt16LE(1,22);h.writeUInt32LE(sr,24);h.writeUInt32LE(sr*2,28);h.writeUInt16LE(2,32);h.writeUInt16LE(16,34);h.write('data',36);h.writeUInt32LE(pcm.length,40);fs.writeFileSync(`profiler/dist/audio/${id}.wav`,Buffer.concat([h,pcm]))}console.log('Generated 10 original dark ambient tracks.');
